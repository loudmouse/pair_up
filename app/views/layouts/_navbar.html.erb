<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">PairUp</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link" <%= link_to "Home", root_url %></a>
      <a class="nav-item nav-link" <%= link_to "Events", events_path %></a>
      <% if user_signed_in? %>
        <a class="nav-item nav-link" <%= link_to "New Event", new_event_path %></a>
      <% end %>
      <% if !user_signed_in? %>
        <a class="nav-item nav-link" <%= link_to "Sign In", new_user_session_path %></a>
        <a class="nav-item nav-link" <%= link_to "Sign Up", new_user_registration_path %></a>
      <% end %>
    </div>
  </div>
</nav>
